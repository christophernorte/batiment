{% extends '::adminBase.html.twig' %}

{% block body -%}
    <h1>Commentaire</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Text</th>
                <td>{{ entity.text }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Isaffiche</th>
                <td>{{ entity.isaffiche }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('secured_commentaire') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <a href="{{ path('secured_commentaire_edit', { 'id': entity.id }) }}">
            Editer
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
