{% extends '::adminBase.html.twig' %}

{% block body -%}
    <h1>Edition</h1>

    {{ form_start(edit_form) }}
	
	{{ form_errors(edit_form.commentaire) }}
	{{ form_label(edit_form.commentaire) }}
	{{ form_widget(edit_form.commentaire, {'attr': {'rows': '10','cols': '60'} }) }}<br />
	
	{{ form_errors(edit_form.isaffiche) }}
	{{ form_label(edit_form.isaffiche)  }}
	{{ form_widget(edit_form.isaffiche) }}<br />
	
	{{ form_errors(edit_form.idrubrique) }}
	{{ form_label(edit_form.idrubrique)  }}
	{{ form_widget(edit_form.idrubrique) }}<br />
	
	{{ form_errors(edit_form.image) }}
	{{ form_label(edit_form.image)  }}
	{{ form_widget(edit_form.image) }}<br />
	
	{{ form_widget(edit_form.submit) }}
	
	{{ form_end(edit_form) }}

    <ul class="record_actions">
    <li>
        <a href="{{ path('secured_photo') }}">
            Retour à la liste
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}