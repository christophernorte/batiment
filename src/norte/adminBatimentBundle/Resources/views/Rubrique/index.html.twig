{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Rubrique list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Commentaire</th>
                <th>Adresse</th>
                <th>Createdat</th>
                <th>Updatedat</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('secured_rubrique_show', { 'id': entity.id }) }}">{{ entity.nom }}</a></td>
                <td>{{ entity.commentaire }}</td>
                <td>{{ entity.adresse }}</td>
                <td>{% if entity.createdAt %}{{ entity.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.updatedAt %}{{ entity.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('secured_rubrique_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('secured_rubrique_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('secured_rubrique_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
